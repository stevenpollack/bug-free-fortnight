# Copy to .env.local for local development
# In production (Docker), the API and web are served from the same origin,
# so VITE_API_BASE_URL should be left unset or set to an empty string.
VITE_API_BASE_URL=http://localhost:3001
